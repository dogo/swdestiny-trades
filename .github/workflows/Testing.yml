name: Testing
on: [push, pull_request]

env:
  CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

jobs:
  test:
    name: Testing SWDestiny Trades app
    runs-on: macos-26
    strategy:
      matrix:
        scheme: [
          SWDestinyTrades
        ]
        workspace: [
          swdestiny-trades.xcworkspace
        ]
        destination: [
          'platform=iOS Simulator,name=iPhone 17 Pro,OS=26.2'
        ]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          lfs: true

      - name: Select Xcode
        run: sudo xcode-select -switch /Applications/Xcode_$(cat .xcode-version).app

      - name: Restore cache
        id: cache-restore
        uses: actions/cache/restore@v6
        with:
          path: Tuist/.build
          key: ${{ runner.os }}-app-${{ hashFiles('Package.resolved') }}

      - name: Install Project Tools
        uses: jdx/mise-action@v4
        with:
          experimental: true

      # Starts the gRPC socket proxy that Xcode's compilation cache (CAS) connects
      # to. Without this step the project is wired to a remote CAS socket that
      # never exists -> `CAS error: deadlineExceeded(... No such file or directory)`.
      - name: Tuist Cache
        if: github.event.pull_request.head.repo.fork != true
        run: tuist setup cache

      - name: Install Tuist dependencies
        if: steps.cache-restore.outputs.cache-matched-key == ''
        run: tuist install

      - name: Generate project with tuist
        # Fork PRs skip `tuist setup cache` (no secrets), so don't wire the
        # project to the remote CAS socket — it won't exist on those runners.
        env:
          TUIST_DISABLE_XCODE_CACHE: ${{ github.event.pull_request.head.repo.fork == true && '1' || '' }}
        run: |
          tuist generate --no-open
        shell: bash

      - name: Test ${{ matrix.scheme }} on ${{ matrix.destination }}
        run: |
          xcodebuild \
            -workspace "${{ matrix.workspace }}" \
            -scheme "${{ matrix.scheme }}" \
            -destination "${{ matrix.destination }}" \
            -resultBundlePath ./coverage/SwdUnitTests.xcresult \
            -enableCodeCoverage YES \
            COMPILER_INDEX_STORE_ENABLE=NO \
            test | xcpretty && exit ${PIPESTATUS[0]}

      # Convert the app-scoped .xcresult into Cobertura XML ourselves so Codecov
      # reports app coverage. Codecov's own xcresult auto-discovery picked up the
      # test bundle instead of the app target.
      - name: Convert coverage to Cobertura
        run: |
          brew install xcresultparser
          xcresultparser --output-format cobertura \
            --project-root "$PWD" \
            ./coverage/SwdUnitTests.xcresult > ./coverage/cobertura.xml

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v7
        with:
          files: ./coverage/cobertura.xml
          disable_search: true
          # Disable the built-in xcode plugin: its `llvm-cov` pass fails on the
          # app binary and only produces the xctest bundle's coverage, which
          # would otherwise be uploaded alongside (and contaminate) our report.
          plugins: noop
          verbose: true

      - name: Upload Snapshot Failures
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: snapshot-failures
          path: SWDestinyTradesTests/FailureDiffs/

      - name: Save cache
        id: cache-save
        uses: actions/cache/save@v6
        with:
          path: Tuist/.build
          key: ${{ steps.cache-restore.outputs.cache-primary-key }}
